import React from 'react';

const NoMatch = () => {
    return (
        <div style={{
            textAlign: "center"
        }}>
            <h1>Page Not Found || <span style={{
                color: "red"
            }}>404</span> ERROR</h1>
        </div>
    );
};

export default NoMatch;